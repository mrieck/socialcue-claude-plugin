// AUTO-GENERATED by socialcue-shared/scripts/sync.js — do not edit. Edit ../../../socialcue-shared/src and re-run `npm run sync`.
/**
 * @socialcue/shared — the single-source-of-truth "brain".
 * Vendored into the plugin and the extension via scripts/sync.js.
 */
export * from './prompts.js';
export * from './platforms.js';
export * from './normalize-url.js';
export * from './schema.js';
export * from './freshness.js';

// The path to the static scoring guidance, for consumers that render it
// (the plugin Skill body; the extension's system prompt).
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
export const SCORING_CORE_PATH = path.join(__dirname, 'scoring-core.md');
